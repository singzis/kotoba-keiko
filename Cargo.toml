[package]
name = "kotoba-keiko"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "keiko"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
rand = "0.9"
rusqlite = { version = "0.32", features = ["bundled"] }
